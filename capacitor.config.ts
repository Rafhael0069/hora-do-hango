import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rafhael.dev',
  appName: 'Hora do Rango',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
