import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Hora do Rango',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
